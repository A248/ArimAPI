<project xmlns="http://maven.apache.org/POM/4.0.0"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
	<modelVersion>4.0.0</modelVersion>
	
	<parent>
		<groupId>space.arim.api</groupId>
		<artifactId>arimapi-parent</artifactId>
		<version>0.19.11-SNAPSHOT</version>
	</parent>
	
	<artifactId>arimapi-env</artifactId>
	<packaging>pom</packaging>
	<name>ArimAPI-Env</name>
	<description>The environmental parent component of ArimAPI.</description>
	
	<properties>
		<spigot.version>1.8.8-R0.1-SNAPSHOT</spigot.version>
		<bungeecord.version>1.16-R0.3</bungeecord.version>
		<sponge.version>7.2.0</sponge.version>
		<velocity.version>1.1.0-SNAPSHOT</velocity.version>
		
		<plugin.name>ArimAPI</plugin.name>
		<plugin.annotationId>arimapi</plugin.annotationId> <!-- Used for @Plugin annotation on Sponge and Velocity -->
		<plugin.version>${project.version}</plugin.version>
		<plugin.url>${project.parent.url}</plugin.url>
		<plugin.description>${project.description}</plugin.description>
	</properties>
	
	<modules>
		<module>core</module>
		<module>plugin</module>
	</modules>
	
	<dependencyManagement>
		<dependencies>			
			<dependency>
				<groupId>org.spigotmc</groupId>
				<artifactId>spigot-api</artifactId>
				<version>${spigot.version}</version>
			</dependency>
			<dependency>
				<groupId>net.md-5</groupId>
				<artifactId>bungeecord-api</artifactId>
				<version>${bungeecord.version}</version>
			</dependency>
			<dependency>
				<groupId>net.md-5</groupId>
				<artifactId>bungeecord-chat</artifactId>
				<version>${bungeecord.version}</version>
			</dependency>
			<dependency>
				<groupId>org.spongepowered</groupId>
				<artifactId>spongeapi</artifactId>
				<version>${sponge.version}</version>
			</dependency>
			<dependency>
				<groupId>com.velocitypowered</groupId>
				<artifactId>velocity-api</artifactId>
				<version>${velocity.version}</version>
			</dependency>
		</dependencies>
	</dependencyManagement>
	
	<repositories>
		<repository>
			<id>spigot-repo</id>
			<url>https://hub.spigotmc.org/nexus/content/repositories/snapshots/</url>
		</repository>
		<repository>
       		<id>bungeecord-repo</id>
			<url>https://oss.sonatype.org/content/repositories/snapshots</url>
   		</repository>
   		<repository>
			<id>sponge-repo</id>
			<url>https://repo.spongepowered.org/maven</url>
		</repository>
		<repository>
			<id>velocity</id>
			<url>https://nexus.velocitypowered.com/repository/maven-public/</url>
		</repository>
	</repositories>
</project>
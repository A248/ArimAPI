/**
 * Usable implementations of {@link Configuration}. <br>
 * <br>
 * The main implementations, {@link MergingConfig} and {@link PairedConfig}, are built through {@link ConfigurationBuilder}.
 * {@link SingularConfig} is a wrapper which may be constructed from any {@code Configuration}.
 *
 * @deprecated See deprecation of {@link space.arim.api.configure} (this entire framework is deprecated)
 */
package space.arim.api.configure.configs;

import space.arim.api.configure.Configuration;